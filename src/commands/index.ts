import author, { author as authorAction } from "./author";
import maintainer, { maintainer as maintainerAction } from "./maintainer";
import evaluate, { evaluate as evaluateAction } from "./evaluate";

export {
  author,
  authorAction,
  maintainer,
  maintainerAction,
  evaluate,
  evaluateAction
}
